#include "Generated.xcconfig"

// === Rust Static Lib Linker Config ===
// === 防剥离配置 ===
// 1. 关闭部署后处理
DEPLOYMENT_POSTPROCESSING = NO

// 2. 禁止剥离已安装的产品
STRIP_INSTALLED_PRODUCT = NO

// 3. 链接器配置
OTHER_LDFLAGS = $(inherited) -force_load "$(PROJECT_DIR)/libs/libnovella_native.a" -lc++
